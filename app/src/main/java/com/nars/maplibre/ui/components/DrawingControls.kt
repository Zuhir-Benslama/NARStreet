package com.nars.maplibre.ui.components

import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier

/**
 * Drawing controls toolbar.
 *
 * Placeholder — implement when Geoman UI integration is ready.
 * The web version (nars-vite) renders these controls via the Geoman JS SDK's
 * built-in toolbar. The Android Geoman port should provide equivalent UI.
 *
 * TODO: Implement Geoman drawing toolbar (draw, edit, delete, snap)
 *   - Draw controls: polygon, polyline, circle, marker
 *   - Edit controls: vertex drag, rotate, scale
 *   - Action controls: undo, redo, commit, cancel
 */
@Composable
fun DrawingControls(
    modifier: Modifier = Modifier
) {
    // TODO: Implement drawing toolbar
}

@Composable
fun FloatingDrawingControls(
    modifier: Modifier = Modifier
) {
    // TODO: Implement floating drawing toolbar
}
