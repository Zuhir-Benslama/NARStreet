package com.nars.narstreet.ui.theme

import androidx.compose.ui.graphics.Color

// Seed: NARS teal-green — maps to M3 primary via dynamic color on API 31+
val NarsTeal      = Color(0xFF1D9E75)
val NarsTealDark  = Color(0xFF0F6E56)
val NarsOnTeal    = Color(0xFFFFFFFF)

// Status colors (not overridden by dynamic color)
val SyncPending   = Color(0xFFBA7517)
val SyncError     = Color(0xFFE24B4A)
val SyncSuccess   = Color(0xFF1D9E75)
val SyncOffline   = Color(0xFF888780)
