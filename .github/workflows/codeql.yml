name: "CodeQL"

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: "0 6 * * 1"

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      security-events: write
      packages: read
      actions: read
      contents: read
    env:
      GEOMAN_DIR: ${{ github.workspace }}/maplibre-geoman-android/app

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Checkout maplibre-geoman-android
        uses: actions/checkout@v7
        with:
          repository: Zuhir-Benslama/maplibre-geoman-android
          ref: main
          path: maplibre-geoman-android

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: 21
          distribution: temurin

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Create local.properties
        run: cp local.properties.example local.properties

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: java-kotlin

      - name: Build with Gradle
        run: ./gradlew assembleDebug --no-daemon

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3
